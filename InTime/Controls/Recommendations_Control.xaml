<UserControl x:Class="InTime.Controls.Recommendations_Control"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:InTime.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <Style x:Key="title_tb" TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="VerticalAlignment" Value="Top"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property="Margin" Value="50,50,0,10"/>
            <Setter Property="FontSize" Value="36"/>
            <Setter Property="FontFamily" Value="Segoe UI Semibold"/>
        </Style>
    </UserControl.Resources>
    <Grid Background="#FF2E2A2A">
        <Grid.RowDefinitions>
            <RowDefinition Height="100"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <TextBlock Text="Welcome" VerticalAlignment="Center" HorizontalAlignment="Center"
                   FontSize="64" Foreground="White" FontFamily="Franklin Gothic Demi"/>
        <StackPanel Name="tape_panel" Grid.Row="1">
            
        </StackPanel>
    </Grid>
    
</UserControl>
